<h1>Lista de Sedes</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Nombre</th>
      <th>dirección</th>
      <th>Telefono</th>
    </tr>
  </thead>
  <tbody>
    {{#each sedes}}
    <tr>
      <td>{{this.id}}</td>
      <td>{{this.name}}</td>
      <td>{{this.address}}</td>
      <td>{{this.telephone}}</td>
      {{!-- <td>
        <a href="/products/edit/{{this.id}}">Editar</a>
        <form action="/products/delete/{{this.id}}" method="POST" style="display:inline;">
          <button type="submit">Eliminar</button>
        </form>
      </td> --}}
    </tr>
    {{/each}}
  </tbody>
</table>

<a href="/sedes/new">Crear Nueva sede</a>
